const myObj = {
    js : 'JavaScript',
    py : 'Python',
    cpp : 'C++'
}
for (const key in myObj) {
    console.log(`Full form of ${key} is: ${myObj[key]}`);
    
}


const array02 = [1, 2,5 ,3, 4]
for (const key in array02) {
    console.log(`My Array Is : ${array02[key]}`);
    // console.log(array02[key]);
    
    
}